# 构画更新说明

## 3.7.11（2026-09-23）

### 收藏按钮兼容

- 有旧楼外显样本时，新楼收藏按钮直接显示在编辑按钮同一栏；没有样本时仍交由菜单精简器处理。

### 面内讨论读取记忆

- 面内 AI 讨论会读取当前所选记忆源。选择千千结时读取轻量前情与召回结果；材料缺失会明确提示，且不会发送给模型。
- 切换记忆源或聊天时，会取消迟到结果。

## 3.7.10（2026-09-22）

### 收藏入口

- 收藏按钮可分别放在楼层原位置和消息“…”菜单，也可以两处同时显示或全部关闭；切换入口位置不会删除已有收藏。
- 修复菜单精简器刷新时收藏按钮短暂闪现的问题，并避免收藏按钮与精简后的菜单图标重叠；同时兼容宿主主题的菜单布局。

### 自定义历法

- 主楼时间戳会独立读取当前聊天的自定义月名与每月天数，不修改用户自定义的时间戳提示词；默认公历仍沿用原有提示。
- 自定义历法日期越界时，轴显示“日期待确认”，楼内日期回退到已经确认的合法锚点；不再原样显示越界日期，也不会擅自推算新日期。

## 3.7.9（2026-09-22）

### 内置记忆

- 长期记忆会纳入隐藏的 AI 楼；“检查完整性”会直接列出 AI 楼、稳定组、已完成、待补、失败与净化后为空的数量。
- “补齐缺失”和“推翻重构”只在保存完成后计入成功进度；生成失败、中止、保存失败或保存结果无法确认时会明确提示。重构在保存阶段中止时，会按实际保存状态保留结果或还原旧记忆。

### “间”的结构化卡片

- “轴卡片／轴历卡片”按具体日期的“历”卡片处理；明确提到历法、纪年或月份结构时才生成整套历法卡。帮助提问与否定指令不会误触发写卡。
- 生成请求会记住本轮预期的卡片类型。AI 缺少标签、返回错类或给出无效卡片时，原回复仍会保留，并显示无法应用的原因；混合回复中的额外错类卡片不会开放应用。

### 线的剧情倾向

- 设置新增按角色保存的“自然发展、温暖向好、冲突增强、悲剧倾向”。它会参与线的生成、推进、“间”生成线卡，以及开启后的线潜伏注入。
- 剧情倾向只在符合现有证据的走向中调整优先级，不强制结果，并可与成人剧情模式同时使用。

### 交互修复

- 悬浮球拖动在触控取消、指针捕获丢失或位置保存失败后会正常结束；拖动后的点击只拦截一次，并继续使用 5px 拖动阈值。
- 时间校准编辑行会随面板宽度换行，保存与取消按钮保持完整宽度。

## 3.7.8（2026-09-22）

### 千千结记忆读取

- 构画改用千千结提供的轻量提示词快照，只读取已经准备好的前情与召回材料。
- 同一聊天、角色和 Persona 的最近一次成功结果会持久保存；页面刷新后千千结暂未就绪或暂时无结果时，可以继续使用这份材料。明确错误、接口不可用、取消、切换聊天或身份不一致时不会复用。

## 3.7.7（2026-09-16）

### 线的历史版本

- 历史版本弹窗在列表较长时会滚动显示选项，当前版加 10 份旧版也不会把底部“关闭”和“预览”按钮挤出屏幕。

### 原生酒馆导入

- 修复原生 SillyTavern 1.18 普通存储导入构画数据时出现 `unsupported-core-contract` 的兼容问题，支持单聊和群聊。
- 只替换勾选的模块，保留未选模块、聊天正文和其他聊天设置；保存后重新读取原目标聊天核对结果。检测到未保存改动或数据冲突时会停止导入，无法确认保存结果时会提示核实。

## 3.7.6（2026-09-14）

### 近期正文读取

- “间”取消每楼 2500 字符截断，读取最近 6 个可见、非空 AI 楼的完整清洗正文。
- 修复正文套在多层包裹符中时，已设为保留的内容仍被误删的问题；只保留 content 或同时保留多层标签均可正确读取，保留项的填写顺序不影响结果。
- 未保留的思考块、状态栏等内容与注释仍按现有规则清理。
- 更新并刷新后，再向“间”发一条消息即可重新读取已有楼层，无需逐楼编辑或重新初始化。

## 3.7.5（2026-09-14）

### 线的历史版本

- 线主页“重新生成”左侧新增历史版本入口；保留最近 10 份旧版，当前版另计，并按真实生成时间从新到旧排列。旧数据无法确认生成时间时会如实标明未知。
- 选择历史版本后可以先预览，再确认恢复。恢复的版本会成为当前线，供后台潜伏注入、“当前内容”手动注入和后续更新使用；恢复前的当前版也会保留，可以再次选回。
- 历史从此功能启用后的内容更新开始积累；暂无历史版本时会显示提示。

## 3.7.4（2026-09-14）

### 楼内显示

- 楼内内容没有变化时不再重复重绘，避免已展开的类目意外收起，以及标注池滚动位置跳回顶部；内容真正变化时仍会正常更新。

## 3.7.3（2026-09-14）

### 楼内显示与刻度

- 楼内类目会保持各自的开合状态；展开窗口或类目时会补偿聊天滚动位置，减少标题被顶出视野的情况。
- 首次建立刻度时，会在同一次处理中读取设定与最近现状；即使结果明确为“无”，也会记录已完成初始化，之后不再反复显示初始化状态。

### 历史来源回查

- 来源回查支持全部历史、最近 N 楼（默认 50）和自定真实楼号范围；会读取隐藏剧情，排除用户消息与明确的系统提示。每批最多检查 6 个历史角色回复，批次大小固定，与自动标注间隔独立。回查只为当前待确认事项查找历史来源，不会逐批抽取所有旧事件。
- 成功完成的批次会逐批保存；来源设置、当前聊天、角色和刻度数据都没有变化时，暂停或失败后可按已保存进度恢复，并保护已经存下的内容。待查名单可以直接查看，进度提示改为居中留白，并会说明哪些来源尚未确认及如何手动处理；选择保留待查时，自动标注会暂停，需要先手动处理。
- 候选 ID 带外层括号、来源字段附带说明时也能识别；同一批中的有效条目会正常保存，无效条目单独忽略，并给出更明确的错误提示。

### 自动标注与保存保护

- 自动标注与刻度更新改为串行执行；失败时保留触发计数，删除楼层后也会恢复正确的计数水位。
- 任务进行期间若只追加了用户消息，结果仍可安全保存；切换聊天或角色、改变来源或相关数据后，迟到结果仍会被拦截。保存失败时，不会覆盖期间由用户手动修改的内容。

## 3.7.2（2026-09-13）

### 角色卡排除

- 设置新增“角色卡 → 全局排除”，外观和操作方式与世界书筛选一致，支持搜索、勾选和取消勾选；同名角色卡按各自文件区分。
- 被排除角色卡的单人聊天会完全停用构画，只保留设置入口；悬浮按钮和楼内窗口隐藏，提示词注入停止，生成、记忆与数据管理入口均停用。
- 排除不会修改构画总开关或删除已有的点、线、面、记忆、刻度等数据。取消排除后会重新载入当前聊天并恢复原有内容；若总开关原本关闭，仍保持关闭。
- 群聊不受角色卡排除影响，包括群聊编号为 0 的情况。

### 切换与在途任务

- 启动、切换聊天或新建单聊时，构画不会读取、迁移或重写被排除角色卡的数据。
- 勾选当前角色卡会立即停止正在进行的生成和可中止后台任务，并清除旧界面与注入；迟到的生成、存储检测和记忆读取结果不会在停用后继续生效，已经进入最终保存阶段的数据操作会安全完成。
- 从旧提示进入构画时，被排除角色卡仍只会打开设置，不会露出之前的业务页面。

## 3.7.1（2026-09-13）

### 千千结记忆读取

- 点、线、面开始生成后，会立即显示千千结记忆的读取进度；等待期间可以取消，最长等待 15 秒。
- 同一次生成只读取一次千千结记忆。切换聊天、角色或记忆源后，旧请求的迟到结果不会串入当前内容。
- 千千结尚未就绪、没有可用记忆、读取失败或等待超时时，会在当前功能中说明原因并允许重试。
- 保留原有长期记忆全文，并从新版公开快照补充已建档人物的完整资料与逐楼状态变化；补充内容仍和原记忆一起遵守构画现有的总记忆预算。

### 保存与恢复

- 生成结果保存失败时会如实反馈；迟到的保存事务不会覆盖或回滚期间发生的普通写入、删除或固定角色操作。
- 内置记忆重建会先在独立副本中完成，成功后一次替换；失败或取消时保留原有记忆，不留下半成品。
- 编辑或删除楼层时，只让真正受影响的摘要失效，未受影响的旧记忆继续保留。
- 坐标收藏遇到部分写入、恢复失败或状态不确定时，会显示准确结果，并让缓存跟随已确认的保存状态。

### 点、线与时间

- 同名线会优先按稳定身份定位，避免列表变化后修改到另一条线；旧卡无法确认唯一目标时会明确拒绝误改。
- 点允许地点未知时留空；格式异常的日程分组不再把后续日期整体推歪。
- 修复自定义历法的跨年、星期移动、完整历法字段保留，以及刻度事项编辑时到期年份丢失的问题。
- 线的当前格式、旧格式、全角分隔符和尾随备注继续兼容，单条坏记录不会拖累同批有效记录。

### 生成、解析与界面

- API 流收到标准结束标记后及时收尾；模型已有正文但因长度截断时保留正文，并记录真实截断原因。
- 世界书中的坏条目不会拖累其它有效条目；长记忆按开头、中段、结尾和近期内容更均衡地分配预算。
- 楼内组合窗口的展开状态会在内容重绘、出屏卸载重挂以及最新楼转为历史楼后保持，并在切换聊天时隔离。
- 修复线错误提示被通用清理覆盖、小剧场自定义美化提示未生效，以及“短楼阈值设为 0”不能保留的问题。

### 说明

- 老版本宿主无法确认磁盘落盘时，内部保存结果会保留“未确认”标记；这不代表已经确认写入磁盘。
- 本文件从 3.7.1 开始维护。
